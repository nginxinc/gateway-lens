# Generated from charts/gateway-lens via scripts/generate-manifests.sh. DO NOT EDIT BY HAND.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gateway-lens
  namespace: default
  labels:
    app.kubernetes.io/name: gateway-lens
    app.kubernetes.io/instance: gateway-lens
    app.kubernetes.io/version: "0.2.0"
automountServiceAccountToken: false
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: default-gateway-lens
  labels:
    app.kubernetes.io/name: gateway-lens
    app.kubernetes.io/instance: gateway-lens
    app.kubernetes.io/version: "0.2.0"
rules:
  - apiGroups: ["gateway.networking.k8s.io"]
    resources:
      - gatewayclasses
      - gateways
      - httproutes
      - grpcroutes
      - tlsroutes
      - tcproutes
      - udproutes
      - referencegrants
      - backendtlspolicies
      - listenersets
    verbs: ["watch", "list", "get"]
  - apiGroups: ["apiextensions.k8s.io"]
    resources:
      - customresourcedefinitions
    verbs: ["watch", "list", "get"]
  - apiGroups: [""]
    resources:
      - services
    verbs: ["watch", "list", "get"]
  - apiGroups: ["discovery.k8s.io"]
    resources:
      - endpointslices
    verbs: ["watch", "list", "get"]
  - apiGroups:
      - gateway.nginx.org
    resources:
      - nginxgateways
      - nginxproxies
      - clientsettingspolicies
      - observabilitypolicies
      - upstreamsettingspolicies
      - authenticationfilters
      - proxysettingspolicies
      - ratelimitpolicies
      - wafpolicies
      - snippetsfilters
      - snippetspolicies
      - payloadprocessors
      - externalloadbalancers
    verbs: ["watch", "list", "get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: default-gateway-lens
  labels:
    app.kubernetes.io/name: gateway-lens
    app.kubernetes.io/instance: gateway-lens
    app.kubernetes.io/version: "0.2.0"
subjects:
  - kind: ServiceAccount
    name: gateway-lens
    namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: default-gateway-lens
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-lens
  namespace: default
  labels:
    app.kubernetes.io/name: gateway-lens
    app.kubernetes.io/instance: gateway-lens
    app.kubernetes.io/version: "0.2.0"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway-lens
    app.kubernetes.io/instance: gateway-lens
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-lens
  namespace: default
  labels:
    app.kubernetes.io/name: gateway-lens
    app.kubernetes.io/instance: gateway-lens
    app.kubernetes.io/version: "0.2.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gateway-lens
      app.kubernetes.io/instance: gateway-lens
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gateway-lens
        app.kubernetes.io/instance: gateway-lens
    spec:
      serviceAccountName: gateway-lens
      automountServiceAccountToken: true
      containers:
        - name: gateway-lens
          image: "ghcr.io/nginxinc/gateway-lens:0.2.0"
          imagePullPolicy: IfNotPresent
          args:
            - --port=8080
            - --log-level=info
          ports:
            - name: http
              containerPort: 8080
          resources:
            limits:
              memory: 128Mi
            requests:
              cpu: 50m
              memory: 64Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            runAsGroup: 1001
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            privileged: false
            capabilities:
              drop: ["ALL"]
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
