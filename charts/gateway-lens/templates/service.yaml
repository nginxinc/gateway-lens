apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateway-lens.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gateway-lens.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gateway-lens.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
